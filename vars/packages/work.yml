work:
  - name: AWS CLI
    macosx:
      type: homebrew
      package: awscli

  - name: Android Studio
    macosx:
      type: homebrew_cask
      package: android-studio

  - name: AppGate SDP Client
    macosx:
      type: homebrew_cask
      package: appgate-sdp-client

  - name: ClickUp
    macosx:
      type: homebrew_cask
      package: clickup

  - name: Expo Orbit
    macosx:
      type: homebrew_cask
      package: expo-orbit

  - name: Kubernetes CLI
    macosx:
      type: homebrew
      package: kubernetes-cli

  - name: k9s
    macosx:
      type: homebrew
      package: k9s

  - name: OpenJDK
    macosx:
      type: homebrew
      package: openjdk

  - name: Slack
    macosx:
      type: homebrew_cask
      package: slack
